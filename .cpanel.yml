---
deployment:
  tasks:
    - export DEPLOY_DIR=~/public_html
    - echo "Deploying to $DEPLOY_DIR"
    - /usr/bin/rsync -av --delete --exclude='.git' --exclude='.cpanel.yml' ./ $DEPLOY_DIR/
    - echo "Deployment complete"
